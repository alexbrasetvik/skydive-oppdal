database:
    engines:
        jr:
            engine:
                url: mssql+pyodbc://JR
                # TODO: This is not set correctly by the database provider.
                pool_size: 2
                max_overflow: 1

plugins:
  bundles:
    jr:
      - jr


includes:
    - sync-client.yaml
    - site.yaml
